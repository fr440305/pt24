# PT24: A 24-GAME SOLVER

## Build

Requirements: `gcc`, `bash`.

How: `./build`

## Run

```
./a.out -parenthesis 2 2 3 3    # ((2 + 2) * (3 + 3))
./a.out -h    # Show helps
```

See ./screenshot.png for output.
